export type bonusCardType = {
    id: number,
    casinoName: string,
    bgColor: string,
    bonusType: number,
    upToPercent: number,
    upToSum: number,
    img: string,
    freeSpinsCount: number,
    exclusive: boolean,
    slotName: string
}